package com.shivam.learningapp.interfaces

interface CustomListener {
    fun setVisibility(visibility: Int,lottieAnimeSuccess :Int)

    fun setVisibilityAnimSwipe(visibility: Int,lottieAnime :Int)

    fun setVisibilityAnim(visibility: Int,lottieAnimeSuccess :Int)

    fun changeText(View: Int)
}